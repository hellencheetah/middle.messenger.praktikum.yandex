---
styles: './edit-password.scss'
form:
    -
        type: 'password'
        placeholder: 'Old password'
        modifier: 'form-control--with-label'
        label: 'Old password'
        name: 'oldPassword'
    -
        type: 'password'
        placeholder: 'New password'
        modifier: 'form-control--with-label'
        label: 'New password'
        name: 'newPassword'
    -
        type: 'password'
        placeholder: 'New password again'
        modifier: 'form-control--with-label'
        label: 'New password again'
        name: 'confirmPassword'

btn-text: 'Save'
btn-class: 'edit-password__btn'
---

{{#extend "main"}}
    {{#content "main"}}
        <div class="edit-password">
            <div class="container container--auth">
                <div class="edit-password-inner">
                    <form class="edit-password__form">
                        {{#each form}}
                            {{> input this }}
                        {{/each}}

                        {{> button  }}
                    </form>
                </div>
            </div>
        </div>
    {{/content}}
{{/extend}}
